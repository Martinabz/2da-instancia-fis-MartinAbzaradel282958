export class Profesores {
  unNombre;
  unaCedula;
  unaDireccion;
  unIngreso;

  constructor(unNombre, unaCedula, unaDireccion, unIngreso) {
    this.unNombre = unNombre;
    this.unaCedula = unaCedula;
    this.unaDireccion = unaDireccion;
    this.unIngreso = unIngreso;
  }
}
